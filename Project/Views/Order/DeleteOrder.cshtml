@model Project.Models.Order

@{
    ViewBag.Title = "DeleteOrder";
}
    <style>
        .body-content {
            padding-left: 60px;
            padding-right: 60px;
            padding-top: 135px;
        }

        .footer {
            position: fixed !important;
            left: 0;
            bottom: 0;
            width: 100%;
            z-index: 1;
        }
    </style>
<h2>Delete Order</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Order</h4>
    <div style="margin:3px;border:2px solid blue">
        Description: @Model.Description
        <br />
        DateTime:  @Model.Date
        <br />
        Amount :@Model.Amount
        <br />
        Address :   @Model.Address

    </div>
    @Html.ActionLink("Go to saveDelete", "SaveDeleteOrder", "Order", new { Model.Id }, new { @class = "btn btn-primary" })
</div>

@section scripts{
    <script src="~/Scripts/myscript/custom.js"></script>
}
